//import {forEach, filter, includes, head} from 'lodash';
//import {winningCombinations} from './../utils/helpers';

export default {
    /**
     * The current winning combination.
     *
     * @param state
     */
    // winningCombination: state => {
    //     forEach(winningCombinations, combination => {
    //         let cells = filter(state.game.cells, cell => includes(combination, cell.location));
    //
    //         let firstCell = head(cells);
    //
    //         if (firstCell.value === null) {
    //             continue;
    //         }
    //
    //         forEach(cells, cell => {
    //             if (cell.value === null || cell.value !== firstCell.value) {
    //             }
    //         });
    //     });
    // }
}
